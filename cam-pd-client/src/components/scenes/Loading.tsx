import { Component } from 'react';
import styled from 'styled-components';

const Layout = styled.div``;

class LoadingScene extends Component<any, any> {
  public render() {
    return <Layout>Scene Loading</Layout>;
  }
}

export default LoadingScene;
